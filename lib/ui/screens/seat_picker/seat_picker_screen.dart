import 'package:flutter/material.dart';

class SeatPickerScreen extends StatelessWidget {
  const SeatPickerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: Text("Seat Picker"),
    ),
    body: Center(child: Text("hhhhhh")),
    );
  }
}
